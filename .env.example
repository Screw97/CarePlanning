# Copy to .env (which is gitignored) and fill in. ONLY the anon key belongs in the app.
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# NEVER put the service_role key here or anywhere the app can reach it.
# SUPABASE_SERVICE_ROLE_KEY=  <-- do not add; server-only, kept out of this repo
